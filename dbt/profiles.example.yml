# Copy to ~/.dbt/profiles.yml
orly_live:
  target: dev
  outputs:
    dev:
      type: bigquery
      method: oauth
      project: 
      dataset: paris_orly_dbt
      location: EU
      threads: 4
      timeout_seconds: 300
