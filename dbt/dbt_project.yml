name: "orly_live"
version: "1.0.0"
config-version: 2

profile: "orly_live"

model-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  orly_live:
    staging:
      +materialized: view
    marts:
      +materialized: table
